'use strict';

var chai = require("chai");
var expect = require('chai').expect


describe('Array', function(){
  describe('#indexOf()', function(){
    it('should check that a number is equal to another number', function(){
        expect(1).to.equal(1);
        expect(3).to.equal(3);
    })
  })
});
